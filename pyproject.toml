[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tensorvid"
version = "0.1.0"
description = "A zero-copy fast video loader for PyTorch"
dependencies = [
    "torch",
    "numpy"
]
